name: munus
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+2

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  go_router: ^17.3.0
  flutter_riverpod: ^3.3.2
  shared_preferences: ^2.5.5
  yaml: ^3.1.3
  wakelock_plus: ^1.6.1
  qr_flutter: ^4.1.0
  package_info_plus: ^10.1.0
  tutorial_coach_mark: ^1.3.3


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  assets:
    - assets/rituals/bendiciones/bendicion_del_agua_fuera_de_la_misa.yaml
    - assets/rituals/bendiciones/bendicion_de_una_nueva_casa.yaml
    - assets/rituals/bendiciones/bendicion_medalla_san_benito.yaml
    - assets/rituals/bendiciones/exorcismo_bendicion_sal_y_agua.yaml
    - assets/rituals/adoracion/letanias_de_desagravio.yaml
    - assets/rituals/adoracion/oracion_antes_de_la_bendicion_eucaristica.yaml
    - assets/rituals/bendiciones/bendicion_e_imposicion_del_escapulario.yaml
    - assets/rituals/sacramentos/indulgencia_plenaria_articulo_mortis.yaml
    - assets/rituals/difuntos/responso_por_los_difuntos_iii.yaml
    - assets/rituals/vesticion/oraciones_de_vesticion.yaml
    - assets/rituals/bendiciones/bendicion_objetos_piadosos.yaml
    - assets/rituals/bendiciones/bendicion_laboratorio_taller_tienda.yaml
    - assets/rituals/bendiciones/bendicion_vehiculo_y_barca.yaml
    - assets/rituals/sacramentos/uncion_cap2_rito_ordinario.yaml
    - assets/rituals/difuntos/ultimo_adios_al_cuerpo_del_difunto.yaml
    - assets/rituals_ar/bendiciones/bendicion_del_agua_fuera_de_la_misa.yaml
    - assets/rituals_ar/bendiciones/bendicion_de_una_nueva_casa.yaml
    - assets/rituals_ar/bendiciones/bendicion_laboratorio_taller_tienda.yaml
    - assets/rituals_ar/bendiciones/bendicion_vehiculo_y_barca.yaml
    - assets/rituals_ar/bendiciones/bendicion_objetos_piadosos.yaml
    - assets/rituals_ar/bendiciones/bendicion_e_imposicion_del_escapulario.yaml
    - assets/images/tiritas.png
    - assets/images/tirita_sola.png

  fonts:
    - family: CormorantGaramond
      fonts:
        - asset: assets/fonts/CormorantGaramond-VariableFont_wght.ttf
        - asset: assets/fonts/CormorantGaramond-Italic-VariableFont_wght.ttf
          style: italic
    - family: SourceSerif4
      fonts:
        - asset: assets/fonts/SourceSerif4-VariableFont_opsz,wght.ttf
        - asset: assets/fonts/SourceSerif4-Italic-VariableFont_opsz,wght.ttf
          style: italic
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-VariableFont_opsz,wght.ttf
        - asset: assets/fonts/Inter-Italic-VariableFont_opsz,wght.ttf
          style: italic
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/Munus_icon.png"
  adaptive_icon_background: "#F8F6F1"
  adaptive_icon_foreground: "assets/images/Munus_icon.png"
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
